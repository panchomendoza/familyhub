[build]
builder = "DOCKERFILE"
dockerfilePath = "../../Dockerfile"

[deploy]
startCommand = "pnpm --filter @familyhub/api dev:start"
healthcheckPath = "/_health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
